<% include ./partials/header %>
<title>ClassStats: Professors</title>
</head>

<body>
    <% include ./partials/navbar %>
    <% include ./partials/announcement %>
    <h1 class="pageHeaders">Professors</h1>

<!-- WORK ON THIS -->

    <% print.forEach(function(professors) { %>
        <div class="pageLinks">
            <a class="" href="/courses/<%= courses.Name %>"><%= professors.Lname %></a>
        </div>
    <% }) %>
</body>

</html>