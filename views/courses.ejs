<% include ./partials/header %>
<title>ClassStats: Courses</title>
</head>

<body>
    <% include ./partials/navbar %>
    <% include ./partials/announcement %>

    <!-- <h1 class="pageHeaders">COURSES</h1> -->

    <!-- <% print.forEach(function(courses) { %>
    <div class="pageLinks">
        <a class="" href="/findcourse/<%= courses.Department %>/<%= courses.Name %>"><%= courses.Name %></a>
    </div>
    <% }) %> -->
    <span id="userInfo" data-name="<%= user.StudentID %>"></span>

    <div class="container">
        <div class="row">
            <div class="col ml-sm-5 ml-0">
                <h1 class="pageHeaders">COURSES</h1>
            </div>
        </div>
        <% print.forEach(function(courses) { %>
        <div class="row pageLinks ml-sm-5 ml-0">
            <a class="" href="/findcourse/<%= courses.Department %>/<%= courses.Name %>"><%= courses.Name %></a>
        </div>
        <% }) %>
    </div>

    <div id="popup"></div>

    <!-- Note: when deploying, replace "development.js" with "production.min.js". -->
    <script src="https://unpkg.com/react@16/umd/react.production.min.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@16/umd/react-dom.production.min.js" crossorigin></script>

    <!-- Load our React component. -->
    <script type="module" src="/PopupRatings.js"></script>
</body>

</html>